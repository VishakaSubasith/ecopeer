query Messages($channelId: Float!) {
  messages(channelId: $channelId) {
    id
    content
    senderId
    isRead
    createdAt
    uploadedFile {
      id
      filename
      storageLocation
    }
  }
}
