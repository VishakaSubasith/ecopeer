query Users ($input: UsersFilterInput!) {
  users(input: $input) {
    id
    email
    solarPowerPlantOwner {
      id
      nickname
    }
    solarPowerPlantMaintainer {
      id
      name
    }
    userType
    accountType
  }
}
